/target
**/*.rs.bk
# Cargo.lock is committed — this is a workspace with a binary.

# secrets / local config
config.toml
.env
.env.*
*.pem
*.key
wallet*.json
keypair*.json

# logs / state
*.log
/data
/state
